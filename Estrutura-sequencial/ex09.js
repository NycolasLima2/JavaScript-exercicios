const prompt = require('prompt-sync')();

console.log("----- Cáuculo do quadrado de um numero inteiro -----");

var V1 = (prompt("insira o valor para ser elevado ao quadrado: "));
let Re = V1 * V1 ;
console.log(`o Resutado é : ${Re}`);